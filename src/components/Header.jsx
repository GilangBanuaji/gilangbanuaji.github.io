import{useEffect,useState}from'react';import{Menu,X}from'lucide-react';import{nav}from'../data/portfolio';export default function Header(){const[o,setO]=useState(false),[s,setS]=useState(false);useEffect(()=>{const f=()=>setS(scrollY>24);addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return <header className={'nav '+(s?'scrolled':'')}><a className="brand" href="#home">GB<span>.</span></a><button className="menu" onClick={()=>setO(!o)} aria-label="Toggle navigation">{o?<X/>:<Menu/>}</button><nav className={'nav-links '+(o?'open':'')}>{nav.map(x=><a key={x} href={'#'+x.toLowerCase()} onClick={()=>setO(false)}>{x}</a>)}</nav></header>}
